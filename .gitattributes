# Auto detect text files and perform LF normalization
*                   text=auto

.gitattributes      text
.gitignore          text

*.md                text diff=markdown merge=union

*.go               -text diff=golang

go.sum              merge=ours

# Don't include in distributions
/.github            export-ignore
.gitattributes      export-ignore
.gitignore          export-ignore